Use this repository as a durable engineering playbook.

When adding knowledge, distinguish verified facts from hypotheses. Prefer a small reproducible check, then record the root cause, smallest safe fix, and regression coverage. Keep guidance portable across repositories and avoid framework-specific prescriptions unless the skill is explicitly scoped.
